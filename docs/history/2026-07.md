# Loro Development History — July 2026

> **Coverage:** 2026-07-25 through 2026-07-29
> **Roadmap:** [`../PLANS.md`](../PLANS.md)  
> **History index:** [`README.md`](./README.md)

Entries are ordered newest first.

### 2026-07-29 - Authenticated Cold Start and Repeat Signup Navigation

- Fixed authenticated cold-start handling so native Supabase auto-refresh begins when the app launches already active, and the auth listener cannot publish `signedOut` while the awaited persisted-session read is still in progress.
- Fixed the login-to-signup transition so the mounted auth screen explicitly selects the signup view every time; returning with the signup back arrow and pressing **New to Loro? Create account** again now reopens the signup form.
- Preserved the existing onboarding import, verification, guest mode, logout, and account-session behavior.
- Verification: `npm run typecheck`, `npm run test:local` (18 tests), Expo web runtime flow check for login → signup → back → signup, and `git diff --check`. Native SecureStore restoration was validated through the startup code path and current Supabase/Expo guidance; a physical-device cold-start test still requires a configured device account.

### 2026-07-29 - Home Quest-First Hierarchy

- Reordered Home around the core daily loop: compact greeting, selected-trail summary and existing 2×3 habit grid, Daily Quest, full Lory briefing, then Adventure Map.
- Replaced the fixed 290 px pre-quest hero with a content-driven briefing card below the quest while preserving the canonical Lory art, generated/fallback copy, four-line bounded message viewport, refresh limits, loading state, and reduced-motion behavior.
- Removed duplicate selected-habit chrome, widened the Home resource lane on compact screens, allowed habit controls to grow to two lines, expanded Home-local refresh/settings targets to 44×44, and exposed trail progress as an accessible progress value.
- Preserved quest mutations, rewards, energy rules, timers, active-habit selection, 2×3 daily-status behavior, guest/authenticated data flow, persistent tabs, celebrations, and Adventure Path navigation.
- Verification: `npm run typecheck`; Impeccable layout detector (`[]`); Expo production web export through Metro/NativeWind (1,090 modules). A rendered 321×677 browser/device comparison was unavailable because the connected browser inventory was empty; the temporary export was removed after verification.

### 2026-07-29 - Onboarding Habit Selector Reference Redesign

- Rebuilt the onboarding habit-selection presentation from the supplied image and HTML reference: compact navigation and progress, centered explanatory copy and selected-count badge, shorter color-coded habit cards, selected-card outlines, a blue-to-white canvas, and a right-aligned Continue arrow.
- Preserved the catalog-driven habit list, arbitrary supported selection count, exact selection order, first-habit derivation, Skip-for-now behavior, persisted onboarding session, accessibility checkbox state, and the shared action button's sound/reduced-motion behavior.
- Added a backward-compatible right-icon position to `QuestActionButton`; all existing callers continue using the default left position.
- Verification: `npm run typecheck`, `npm run test:local` (18 tests), Expo web bundle/runtime inspection, and a direct visual comparison at the reference image's 321×677 mobile viewport. The final layout kept all six habit rows, the Continue action, and Skip visible in the initial viewport.

### 2026-07-29 - Scrollable Lory Briefing Viewport

- Capped the visible Lory briefing message at four lines in the Home card while keeping the heading, refresh action, and loading indicator outside the message viewport.
- Wrapped generated and fallback briefing text in a bounded nested `ScrollView`, so longer messages remain available by scrolling without expanding or reflowing the surrounding Home layout.
- Verification: `npm run typecheck`, `npm run test:local` (18 tests), `git diff --check`, and Expo web runtime inspection at a 390×844 mobile viewport. The rendered message viewport reported an 80px maximum height with automatic vertical overflow.

### 2026-07-29 - Compact Home Resource Capsule

- Redesigned `ResourceBar` and `ResourcePill` from separate expanding cards into one compact warm-white segmented capsule modeled on the provided reference, with thin internal dividers and direct icon/value alignment.
- Positioned the bar explicitly at the top-left of Home while preserving the existing energy, streak, shield, coin, refill-countdown, daily-check-in, reduced-motion, and accessibility behavior. The check-in stays beside the capsule as a separate 44×44 action.
- Added a compact stacked treatment for the unchanged energy refill suffix so all values and the full countdown remain readable at 320-pixel and 390-pixel mobile widths.
- Verification: `npm run typecheck`, `npm run test:local` (18 tests), Expo web bundle/runtime inspection (1,114 modules), and `git diff --check` passed. Live inspection covered 390×844 and 320×720 viewports plus full-energy and low-energy countdown states; the introductory onboarding quest was confirmed restored to its original hold interaction after the isolated guest-preview setup.

### 2026-07-29 - Preserve Selected Habits During Signup

- Fixed the completed onboarding account handoff so the selected habit/order payload is durably written before the signup form can submit.
- Closed the remaining verification race: deep-link session creation, manual verification refresh, and cold-start session restoration now keep authenticated app rendering blocked until the retry-safe onboarding import commits. The first authenticated snapshot therefore reads only the selected habit IDs instead of caching catalog defaults.
- Added pure onboarding-payload regression assertions for exact 2-, 4-, and 6-habit selections, confirming that selection cardinality and order are preserved without filling a subset from catalog defaults. Existing pgTAP coverage verifies the same ordered preference/snapshot behavior through the authenticated import; any unselected habits remain available in Habit Targets for later enabling.
- Verification: `npm run test:local` (18 tests), `npm run typecheck`, the earlier `npm run supabase:test` run (138 pgTAP assertions), and `git diff --check` passed. A live email-delivery/deep-link walkthrough still requires a configured test inbox and device.

### 2026-07-28 - Onboarding Account Signup Handoff

- Fixed the completed onboarding **Create an account** action so it opens the signup view instead of being overwritten by the post-completion sign-in route.
- Preserved the persisted onboarding import payload through signup and email verification; selected habits/order, first habit, and the bounded `+10 XP`, `+10 coins`, and `+1 shield` reward continue to be imported by the authenticated RPC.
- Added active-session view normalization so a later logout still returns to sign-in.
- Verification: `npm run typecheck`, `npm run test:local` (15 tests), Expo web bundle smoke check (1,090 modules), and `git diff --check` passed.

### 2026-07-28 - Completed Onboarding Returns to Login

- Completed onboarding now routes signed-out users directly to the existing login page (`Welcome back, Adventurer!`) instead of reopening the onboarding screens.
- The login page's existing `Continue as guest` action resumes the persisted `local-guest` snapshot after a guest logout; guest sign-out continues to disable only the session flag and does not delete the cache.
- Added a device-local completion marker for verified imports, which is written only after the authenticated onboarding import succeeds. Guest initialization writes the same marker after its local snapshot is created.
- Verification: `npm run typecheck`, `npm run test:local` (15 tests), Expo web bundle smoke check (1,090 modules), and `git diff --check` passed.

### 2026-07-28 - Habit Targets Card Layout Alignment

- Reworked each Habit Targets row to follow the provided compact layout: a single horizontal bordered card with the checkbox at left, habit name above the target controls, and vertically stacked up/down reorder arrows at right.
- Removed the extra habit-icon column from this row so the target line remains readable on narrow mobile screens; retained accessible labels, disabled states, minimum-one-habit protection, and the existing persisted mutation behavior.
- Verification: `npm run typecheck`, Expo web export smoke check (1,090 modules), and `git diff --check` passed.

### 2026-07-28 - Habit Preference Persistence Repair

- Diagnosed the Habit Targets check/uncheck and reorder reversion: the client applied the preference optimistically, but the linked Supabase project was missing the forward migrations that add the preference-aware `update_settings` RPC and snapshot layer. The next authenticated refresh therefore returned the previous full-habit order.
- Deployed the reviewed forward migrations `20260728092827_fix_stale_timed_quest_cleanup.sql`, `20260728120000_onboarding_auth_flow.sql`, `20260728130000_onboarding_starter_reward.sql`, and `20260728140000_habit_preference_mutations.sql`; the local and remote migration ledgers now match through `20260728140000`.
- Added mutation-generation guards around cache/network refreshes in `src/contexts/appContext/index.tsx`, so snapshots or errors started before/during a settings mutation cannot overwrite a newer committed enabled-habit order.
- Verification: `npx supabase db push --dry-run`, `npx supabase db push`, `npx supabase migration list`, `npx supabase db reset --local`, `npx supabase test db --local` (138 assertions), `npm run test:local` (15 tests), `npm run typecheck`, `npm run supabase:lint`, and `git diff --check` passed. Database lint retains only pre-existing Guild/check-in warnings.

### 2026-07-28 - Habit Target Selection and Ordering

- Expanded the More-tab Habit Targets card from target-only editing into the shared habit-preference editor: users can check or uncheck any catalog habit, while a minimum-one-enabled guard prevents an unusable Home state.
- Added accessible up/down reorder controls. The resulting ordered enabled-habit list is persisted through the guest local repository and the authenticated `update_settings` RPC, then consumed by Home and the other shared habit selectors.
- Added forward migration `20260728140000_habit_preference_mutations.sql` with server-catalog validation, ordered preference persistence, authenticated-only execution, and transactional rollback behavior; added pgTAP assertions for reorder, uncheck, invalid/empty lists, and snapshot order.
- Verification: `npm run typecheck`, `npm run test:local` (15 tests), `npx supabase db reset --local`, `npx supabase test db --local` (138 assertions), `npm run supabase:lint`, `npm run supabase:types`, `npx expo export --platform web`, and `git diff --check` passed. Database lint retains only pre-existing Guild/check-in warnings.
- Remaining limitation: the current typed `HabitId`/catalog still requires adding a typed definition when a genuinely new server habit is introduced; the preference UI and persistence path do not impose a fixed selection count.

### 2026-07-28 - Introductory Quest Target and Starter Reward

- Updated the onboarding quest to show the selected habit's first-node target and instruction, then require the existing audio-backed long-press interaction to clear it.
- Added a standalone “Quest cleared” celebration with fixed `+10 XP`, `+10 coins`, and `+1 shield` rewards. Guest initialization applies the bounded reward locally; verified imports receive the same reward through a locked, idempotent Supabase RPC.
- Added reward fields to the onboarding session/import contract, forward migration `20260728130000_onboarding_starter_reward.sql`, generated database types, and pgTAP assertions for one-time reward grants.
- Verification: `npm run typecheck`, `npm run test:local` (15 tests), `npx supabase db reset --local`, `npx supabase test db --local` (129 assertions), and `git diff --check` passed. Database lint retains only pre-existing Guild/check-in warnings.

### 2026-07-28 - IKEA-Style Onboarding and Auth Funnel

- Added the pre-auth landing, catalog-driven habit selection, bounded introductory quest, trail-ready account/guest choice, and accessible guest-retention/migration warnings in `src/screens/onboarding/index.tsx` and `src/navigation/RootGate.tsx`.
- Added persisted onboarding sessions, direct-signup handoff, guest cache initialization, and authenticated import handling through the existing auth provider. Existing-account login remains separate from guest migration.
- Added Supabase migration `20260728120000_onboarding_auth_flow.sql` with ordered `user_habit_preferences`, one-time `guest_onboarding_imports`, snapshot `enabledHabitIds`, RLS, authenticated grants, and idempotent `complete_guest_onboarding` validation. Client-controlled economy, loot, streak, shield, and history values are not imported.
- Added generated pixel-art onboarding illustrations under `src/assets/images/onboarding/`, registered in `src/constants/images.tsx`, plus pure selection utilities and tests for no fixed six-habit ceiling and future catalog entries.
- Verification: `npm run typecheck`, `npm run test:local` (14 tests), `npm run supabase:reset`, `npm run supabase:test` (123 assertions), `npm run supabase:lint`, `npm run supabase:types`, and `git diff --check` passed. Database lint retains only pre-existing Guild/check-in warnings.
- Remaining limitation: the current client `HabitId` union still requires adding typed game definitions when a genuinely new server habit is introduced; the onboarding selector and persistence path do not impose a numeric selection limit.

### 2026-07-28 - Loot Item Details from Daily Quest and Profile

- Made the daily-quest loot preview tappable so the newly earned item opens the shared `InventoryStackDetailsModal`; the loot celebration temporarily hides and restores its exact reward/streak page after the details view closes.
- Added the same item-details interaction to filled equipment slots on Profile, including existing equip/unequip mutations, loading state, and shared inventory-stack grouping. Newly returned loot is included in the stack source when the context snapshot has not rerendered yet.
- Updated Feature #41 in `docs/PLANS.md` to complete. Verification: `npm run typecheck`, `npm run test:local` (11 tests, elevated Windows process permission), and `git diff --check` passed.
- Remaining limitation: item detail interactions were verified statically and through type/local tests; a physical-device modal and screen-reader walkthrough remains recommended before release.

### 2026-07-28 - Stale Authenticated Timed-Quest Cleanup

- Added forward migration `20260728092827_fix_stale_timed_quest_cleanup.sql` to repair `start_daily_quest` after the passive-energy version filtered existing timers by the current date while `active_timed_quests` still enforces one row per user and habit.
- The RPC now locks and evaluates any existing timer for the authenticated user and habit, returns idempotently for today’s matching timer, and removes stale or mismatched timers before charging energy and inserting the replacement.
- Added pgTAP coverage for replacing a prior-local-day Reading timer without a `active_timed_quests_pkey` collision or duplicate energy charge. Verification: `npm run supabase:reset`, `npm run supabase:test` (108 assertions), `npm run supabase:lint`, `npm run supabase:types`, `npm run typecheck`, `npm run test:local`, and `git diff --check` passed.
- Remote migration history was inspected and was already aligned through `20260728080434`; this fix remains a new pending migration and has not been pushed to the shared remote.

### 2026-07-28 - Shield Protection Feedback in Quest Celebration

- Moved shield-consumption feedback out of the app-shell toast path and into the daily quest loot celebration modal's streak page.
- The modal now shows “Streak protected,” explicitly states that one shield was used, and displays the authoritative remaining shield count only when the quest outcome reports an actual consumption.
- Removed the unused shield-specific toast host/provider wiring. Verification: `npm run typecheck` and `git diff --check` passed.
- Remaining limitation: the protection notice is available when the daily quest opens its existing loot celebration; completions without a loot item continue to use the existing no-modal flow.

### 2026-07-28 - Chapter Reward Preview and Unlock Celebration Flow

- Added a `+1 shield` preview to every Adventure Path chapter reward and replaced the path's claimed state with the same reusable `RewardClaimedButton` treatment used by Guild quest cards.
- Added `NewUnlockCelebrationModal` as a reusable, reduced-motion-aware modal for chapter reward claims and profile level-up celebrations. App-shell queueing keeps the chapter/level events sequential and defers them while loot or check-in modals are visible.
- Verification: `npm run typecheck` and `git diff --check` passed.
- Remaining limitation: level-up detection currently observes an authoritative refreshed profile level at the app shell; typed `previousLevel`/`newLevel`/`levelsGained` mutation fields remain a follow-up before Feature #12 can be marked complete.

### 2026-07-28 — Habit Target Source-of-Truth Sync

- Updated `DailyQuestCard` to derive its target summary, timer duration, and one-time quantity label from the same effective-target utility used by local enforcement; selector overrides no longer leave the card's descriptive copy at static defaults.
- Routed More-tab target changes through the normal Context mutation path for both guests and authenticated users. Optimistic Home updates now reconcile with the returned snapshot and roll back on failure; local guest changes are cached as part of the mutation.
- Added forward migration `20260728080434_validate_habit_target_overrides.sql` to validate supported habit IDs, whole-number values, and minimum floors in `update_settings` before persisting JSONB target overrides.
- Added utility and pgTAP coverage for target display, persistence, snapshot propagation, reset-to-default, and invalid minimum values. Verification: `npm run typecheck`, `npm run test:local` (11 tests), `npm run supabase:reset`, `npm run supabase:test` (105 assertions), `npm run supabase:lint`, `npm run supabase:types`, and remote migration dry-run/deployment checks.
- Remaining limitation: one-time quests remain binary confirmations; a customized count is displayed and persisted but is not independently measured by the one-tap interaction.

### 2026-07-28 — Feature #4 Authenticated Streak-Shield Parity

- Added the forward Supabase migration `20260728063726_streak_shield_parity.sql`. It corrects the composed snapshot from the stored profile shield count, increments a shield only after a newly accepted chapter claim, and adds local-date protected-streak recovery to the target-override/passive-energy quest RPC while retaining row locks and duplicate idempotency.
- Added `streakShieldConsumed` and `remainingStreakShields` to shared and local quest outcomes, strict authenticated response parsing, and generated database types.
- Extracted the local shield calculation into `src/utility/streakShield.ts` and added the dependency-free `npm run test:local` suite covering same-day, consecutive-day, date-key month boundaries, missed-day, null-date, both-streak, and single-streak risk cases.
- Added an app-shell UI event queue and non-blocking, accessibility-alert “Streak protected” toast that is independent of the loot celebration modal and auto-dismisses without motion animation.
- Expanded pgTAP coverage from 76 to 100 assertions for remote earning, snapshot parity, protected/unprotected/null-date streak behavior, duplicate retries, resource/activity consistency, loot preservation, and role grants. Verification completed with `npm run typecheck`, `npm run supabase:reset`, `npm run supabase:test`, `npm run supabase:lint`, `npm run supabase:types`, and `git diff --check` (after generated-file cleanup).
- Feature #5 shield purchases remain blocked by the roadmap dependency until this parity gate is deployed and accepted.

### 2026-07-28 — Home Resource Deck Refresh

- Redesigned `ResourceBar` and `ResourcePill` into a responsive two-tier resource deck with themed energy, streak, shield, and coin cards aligned to the app’s blue, red, green, and reward palette.
- The Home screen now keeps the streak-shield counter visible at zero, uses tabular counters and accessible labels, and preserves the energy refill countdown and daily check-in action.
- Verification: `npm run typecheck` and `git diff --check` passed. A physical-device visual walkthrough was not available in this environment.

### 2026-07-28 — One-Time Habit Default Targets

- Changed the canonical one-time defaults to 6 glasses for Water, 8 hours for Sleep, and 10 minutes for Outdoors.
- Shared the client defaults between the quest blueprints and More-tab target controls so guest quests, displayed defaults, and reset behavior cannot drift.
- Added local state hydration normalization so cached guest snapshots use the new quantities and units on Home without altering explicit target overrides.
- Added a forward-only Supabase catalog migration and expanded the pgTAP catalog assertion to keep authenticated snapshots aligned without changing user target overrides.
- Verification: `npm.cmd run typecheck`, clean `npm.cmd run supabase:reset`, `npm.cmd run supabase:test` (76 tests passed), `npm.cmd run supabase:lint` (existing Guild/check-in warnings only), and regenerated Supabase TypeScript types.

### 2026-07-27 — Sleep and Outdoors Target Units

- Changed the one-time Sleep quest unit from `routine` to `hours` and the Outdoors quest unit from `outdoor visit` to `minutes` in `src/constants/habits.ts`.
- Added the forward-only Supabase migration `20260727133857_update_sleep_outdoors_target_units.sql` so existing and newly reset databases return the same units.
- Added a pgTAP assertion covering the canonical Sleep and Outdoors units and updated the Product Guide's habit table.
- Fixed the pre-existing snapshot migration regression by layering passive energy and target overrides onto the latest snapshot wrappers instead of rebuilding the snapshot from the retired `user_inventory` schema.
- Verification: `npm.cmd run typecheck`, clean `npm.cmd run supabase:reset`, `npm.cmd run supabase:lint`, and `npm.cmd run supabase:test` passed; pgTAP completed 76 tests successfully. The linter reported only existing warnings in Guild/check-in RPC code.

### 2026-07-27 — Monthly Development History Archive

- Moved the monolithic conversation log out of `docs/PLANS.md` into the monthly `docs/history/2026-07.md` archive.
- Added `docs/history/README.md` as the archive index and maintenance contract for future `YYYY-MM.md` files.
- Updated the roadmap, agent workflow, product/architecture ownership notes, and README documentation index to reference the monthly history.
- Preserved all 17 prior entries and added this restructure as the eighteenth July entry.
- Validated entry counts, relative links, Markdown structure, and roadmap feature/blueprint integrity.

### 2026-07-27 — Project Documentation Architecture

- Moved the canonical roadmap from `plans/PLANS.md` to `docs/PLANS.md` and preserved all feature IDs, anchors, statuses, blueprints, and historical entries.
- Added `docs/PRODUCT.md` as the source of truth for the current six-habit game loop, product principles, Lory voice, navigation, progression rules, visual direction, and UX boundaries.
- Added `docs/ARCHITECTURE.md` as the source of truth for the current Expo/Supabase stack, data ownership, repository boundaries, Supabase security, caching, date/timezone behavior, accessibility, testing, delivery, and Definition of Done.
- Refocused the root `AGENTS.md` on mandatory workflow, documentation routing, safeguards, commands, verification, and roadmap-maintenance rules.
- Removed the duplicated architecture standards from this roadmap and replaced them with explicit links to the canonical product and architecture documents.
- Added a documentation index to `README.md` and removed the empty legacy `plans/` directory.

### 2026-07-27 — Full Roadmap Architecture and Implementation Review

- Audited all roadmap entries against the current Expo/Supabase architecture, working-tree implementation, migrations, contexts, repositories, screens, and external platform guidance.
- Added a three-state status model and corrected Features #4, #7, #8, #21, #28, #35, and #42 to partial where foundations exist but production behavior is incomplete.
- Added cross-cutting architecture, data ownership, security/RLS, client organization, accessibility/motion, testing, and Definition of Done standards.
- Added detailed implementation blueprints for every incomplete/partial feature, including client/domain/backend boundaries, guest parity, offline behavior, security, dependencies, and verification.
- Promoted launch readiness/observability/security/QA to Feature #43 and asset optimization/bundle budgets to Feature #44.
- Recorded immediate engineering debt: authenticated streak-shield parity/snapshot regression, target-override validation/races, duplicate date utilities, missing unit/component harness, and snapshot growth.
- Reworked the dependency phases so completed features are no longer scheduled and high-risk economy, social, native, AI, and payment work cannot bypass their foundations.
- Preserved all prior conversation history and completed-feature implementation notes.

### 2026-07-26 — Feature #4 Implementation (Streak Shield Mechanics)

- Added `isStreakReset` helper in `src/utility/adventurePath.ts` to detect when a streak gap exceeds one day.
- Updated `claimLocalChapterReward` in `src/services/localGameRepository.ts` to increment `inventory.streakShields` by 1 on each chapter reward claim.
- Updated `completeLocalDailyQuest` to check if habit or daily streak would reset. If `streakShields > 0`, consumes exactly one shield and preserves both streaks (current + 1) instead of resetting to 1.
- Added shield pill display in `src/components/ResourceBar.tsx` (blue `shield-checkmark` icon + count, shown next to the flame only when shields > 0).
- Added shield count display in `src/screens/profile/index.tsx` next to the streak line in the profile header.
- Design decision: One shield protects all at-risk streaks (habit + daily) for a single quest completion. "Streak Protected!" toast deferred as optional future enhancement.

### 2026-07-26 — Daily Quest Reset Countdown

- Replaced the completed Daily Quest card's static next-quest subtitle with a live cooldown label in `src/components/DailyQuestCard.tsx`.
- The countdown resolves the next midnight in the user's configured timezone, incorporates the existing server clock offset, and refreshes once per second only while the card is completed or a timed quest is active.
- Kept the broader midnight rollover behavior unchanged; the app's existing provider refresh still owns the actual date transition.

### 2026-07-26 — Daily Quest Cleared Stamp

- Added generated `src/assets/images/cleared-stamp.png`, a distressed red `CLEARED` stamp with transparent bounds.
- Registered the asset in `src/constants/images.tsx` and rendered it on the completed Daily Quest card beside the completion heading.
- Kept the earned coins and XP visible below the stamp and preserved the existing completed-card copy and behavior.

### 2026-07-26 — Shared Habit Status Icon Alignment

- Added `src/components/HabitIconWithStatus.tsx` so Daily Quest, Home's habit selector, and the habit switcher share one primary-icon/status-badge arrangement.
- Moved the Daily Quest badge from the lower-left to the lower-right of the primary habit icon.
- Updated Home's habit selector to preserve each habit's primary icon while displaying completion and timed-quest status as a lower-right overlay.

### 2026-07-26 — Daily Quest Habit Status Icon Alignment

- Updated `src/components/DailyQuestCard.tsx` so the active daily quest shows the active habit icon instead of only the node icon.
- Added the same semantic completion/in-progress status badge arrangement used by the habit switcher, positioned at the lower-left of the primary icon.
- Applied the arrangement to both the active quest and completed quest card states while preserving the existing card dimensions and status colors.

### 2026-07-26 — Feature #3 Status Icon Alignment

- Updated `HabitSwitcher` status badges to preserve each habit's primary icon while overlaying Home's semantic completion/in-progress icon at the lower-right.
- Made the overlay touch-transparent so the parent habit pill remains the only accessible press target.

### 2026-07-26 — Feature #3 Implementation (Habit Quick-Switcher)

- Added `src/components/HabitSwitcher.tsx` and rendered it below the Adventure Path header in `src/screens/home/HabitPathScreen.tsx`.
- The reusable horizontal pill row changes the active habit in place, then refreshes the existing path header, chapter list, node states, and rewards through Context state.
- Pills retain the habit icon and label, with a green completion badge or gold timed-quest badge based on today's local date.
- Kept Home's 2×3 habit grid from Feature #1 as the full daily-status overview; the quick-switcher is deliberately scoped to the Adventure Path where it removes the back-to-Home navigation loop.
- Selection haptics run only when a different habit is chosen and remain controlled by the existing haptics setting.

### 2026-07-26 — Bottom Tab Haptics Correction

- Updated `BottomTabs.tsx`, `PersistentTabHost.tsx`, and `useHaptics.ts`.
- Bottom-tab feedback now uses `selectionAsync()` and fires only when the navigation host accepts an actual tab change.
- Same-tab taps and taps during an in-flight transition no longer produce false tactile confirmation.
- Haptic promise failures are safely ignored so unsupported hardware cannot interfere with navigation or quest interactions.
- Kept swipe and programmatic tab navigation silent; this correction is scoped to direct bottom-bar selection.

### 2026-07-26 — Feature Batch #3 Review

- Reviewed 6 user-proposed features against the codebase and PLANS.md. All 6 were not implemented.
- **Added as new features:**
  - **#41 Tap loot preview in celebration modal for item details (P2):** Make the existing inline loot card tappable to open `InventoryStackDetailsModal` with equip support. No new buttons — tap target is the preview itself.
  - **#42 Item catalog — all collected items (P2):** Read-only gallery in More tab showing every item definition from `discoveredItemDefinitionIds`, including lost/sold items. Grouped by set with completion progress. Distinct from Stash (equip-focused) and Stats Dashboard (#7).
- **Replaced existing feature:**
  - **#28 Custom Habit Creation → Customize Habit Settings:** Downgraded from P3 full custom habit creation to P1 habit setting overrides (type-locked, duration/count only, minimum floor). Original #28 content preserved in conversation log. Custom creation may return later as a P3 feature.
- **Updated existing feature:**
  - **#25 IKEA-Effect Onboarding:** Added explicit implementation note about guest progress migration — reading local guest cache (`"local-guest"`) and seeding Supabase state during sign-up. Explicitly coupled to #25, not a standalone feature.
- **Deferred / not added:**
  - **Community habit marketplace:** Combines #5 (Shop), #28 (Custom Habits), and #30 (Social). All three are unbuilt. Shelved until prerequisites exist.
  - **"Keep guest progress after sign-up":** Already covered by #25 — not a separate feature.

### 2026-07-26 — Feature #6 Implementation (Haptic Feedback)

- Installed `expo-haptics` (SDK 54 compatible) via `npx expo install`.
- Created `src/hooks/useHaptics.ts` — exposes `light()`, `medium()`, `heavy()` functions that read `settings.hapticsEnabled` from the new `useGameSettings()` context hook and call `Haptics.impactAsync()`.
- Added `useGameSettings()` hook to `src/contexts/appContext/index.tsx` — exposes `AppSettings` as a dedicated context slice following the existing 8-slice pattern.
- Wired haptics into 4 components:
  - `BottomTabs.tsx` — `light` on tab press
  - `DailyQuestCard.tsx` — `medium` on quest start and quest complete
  - `QuestCelebrationModal.tsx` — `medium` on loot drop collect (common–epic), `heavy` on legendary drops
  - `InventoryStackDetailsModal.tsx` — `medium` on equip/unequip
- Design decisions: ref-based `hapticsEnabled` check inside memoized callbacks avoids stale closures. Fires haptics synchronously before async actions for immediate tactile feedback.
- Future: `selection` haptic for habit switcher (#3), `heavy` for chapter complete (#36) and level-up (#12) — wired when those celebration variants exist.
- `useGameSettings()` hook also lays the foundation for Feature #8 (Settings UI).

### 2026-07-26 — Feature #2 Implementation

- Implemented energy regeneration countdown timer in `ResourceBar`.
- Added a 30-second tick interval that calculates remaining time until the next energy point (1 energy per 30 minutes).
- Displays "+1 in Nm" suffix on the energy pill when energy is below max and `lastRefillAt` is available.
- Uses `ResourcePill`'s existing `suffix` prop — no component API changes needed.
- Zero-energy fallback and low-energy warning remain as future enhancements to the feature.

### 2026-07-25 — External Recommendations Review (Batch #2)

- Reviewed a second batch of feature/QoL recommendations focused on home screen daily experience, adventure path, profile, stash, guild quests, settings, and global UX polish.
- **Already covered:** Habit completion indicators (#1 — done), daily progress banner (#1 — future enhancement), streak shield UI (#4), level-up celebration (#12), settings screen (#8), haptic feedback (#6), empty states (#22), onboarding (#14), duplicate salvage (#34).
- **Added as new features:** Chapter completion celebration (#36), equipment comparison on equip (#37), "New" badge on items (#38), pull-to-refresh + skeleton loading (#39), guild quest progress toasts + history (#40).
- **Updated existing features:** #1 (N/6 counter + loot rarity teaser), #7 (per-habit breakdown + activity log), #12 (cross-reference to chapter celebration), #15 (visual path map stretch goal).
- **Noteworthy:** The "visual path map" (board game trail aesthetic) is a high-effort/high-impact idea added as a stretch goal to #15. The animated transition between dashboard and HabitPathScreen (1c) and node detail expansion (2a) are valid polish items but too granular for standalone features — they'd be natural byproducts of #15 and #3 respectively.

### 2026-07-25 — External Recommendations Review (Batch #1)

- Reviewed a set of external feature/QoL recommendations against the existing PLANS.md.
- **Already covered:** Energy rework (#2), More tab (#7/#8), quest recovery (#3), calendar (#24), weekly recap (#23), rest days (#27), badges (#11), unused systems (Conflict Analysis), postpone list (aligns with P3 tiering).
- **Added as new features:** Post-completion flow in celebration modal (#33), duplicate gear salvage (#34), adaptive Lory messages (#35).
- **Updated existing features:** #1 (reflected actual implementation), #2 (zero-energy fallback, low-energy warning), #3 (resume quest visibility), #8 (privacy/support/export), #27 (path progress is safe messaging).
- **Added maintenance note:** Guild quest descriptions reference "four habits" but the app now has six — needs updating.

### 2026-07-25 — Feature #1 Implementation

- Implemented today-at-a-glance habit status coloring in the existing `ActiveHabitCard` habit grid (rather than a standalone strip component).
- Each habit pill in the 2×3 grid now shows: blue (active/selected), green (completed today), gold/yellow (timed quest in progress), or muted (unstarted).
- Active/selected state takes visual priority so the user always knows which habit they're viewing.
- Removed the intermediate `DailyHabitStrip` component per user feedback.

### 2026-07-25 — Feature Audit & Roadmap Session

- Conducted comprehensive codebase review across all screens, components, contexts, utilities, constants, and types.
- Identified 24 suggested features/QoL improvements with priority tiers (P0–P3).
- Reviewed user's existing 33 planned items; assessed each for quality, conflict, and alignment.
- Identified one item needing clarification: "Replace quests with gear-attribute mechanics."
- Recommended hybrid approach for post-path-completion content and loot pool management.
- Created this living `PLANS.md` document.
