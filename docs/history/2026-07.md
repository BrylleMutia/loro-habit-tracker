# Loro Development History — July 2026

> **Coverage:** 2026-07-25 through 2026-07-27  
> **Roadmap:** [`../PLANS.md`](../PLANS.md)  
> **History index:** [`README.md`](./README.md)

Entries are ordered newest first.

### 2026-07-27 — Monthly Development History Archive

- Moved the monolithic conversation log out of `docs/PLANS.md` into the monthly `docs/history/2026-07.md` archive.
- Added `docs/history/README.md` as the archive index and maintenance contract for future `YYYY-MM.md` files.
- Updated the roadmap, agent workflow, product/architecture ownership notes, and README documentation index to reference the monthly history.
- Preserved all 17 prior entries and added this restructure as the eighteenth July entry.
- Validated entry counts, relative links, Markdown structure, and roadmap feature/blueprint integrity.

### 2026-07-27 — Project Documentation Architecture

- Moved the canonical roadmap from `plans/PLANS.md` to `docs/PLANS.md` and preserved all feature IDs, anchors, statuses, blueprints, and historical entries.
- Added `docs/PRODUCT.md` as the source of truth for the current six-habit game loop, product principles, Lory voice, navigation, progression rules, visual direction, and UX boundaries.
- Added `docs/ARCHITECTURE.md` as the source of truth for the current Expo/Supabase stack, data ownership, repository boundaries, Supabase security, caching, date/timezone behavior, accessibility, testing, delivery, and Definition of Done.
- Refocused the root `AGENTS.md` on mandatory workflow, documentation routing, safeguards, commands, verification, and roadmap-maintenance rules.
- Removed the duplicated architecture standards from this roadmap and replaced them with explicit links to the canonical product and architecture documents.
- Added a documentation index to `README.md` and removed the empty legacy `plans/` directory.

### 2026-07-27 — Full Roadmap Architecture and Implementation Review

- Audited all roadmap entries against the current Expo/Supabase architecture, working-tree implementation, migrations, contexts, repositories, screens, and external platform guidance.
- Added a three-state status model and corrected Features #4, #7, #8, #21, #28, #35, and #42 to partial where foundations exist but production behavior is incomplete.
- Added cross-cutting architecture, data ownership, security/RLS, client organization, accessibility/motion, testing, and Definition of Done standards.
- Added detailed implementation blueprints for every incomplete/partial feature, including client/domain/backend boundaries, guest parity, offline behavior, security, dependencies, and verification.
- Promoted launch readiness/observability/security/QA to Feature #43 and asset optimization/bundle budgets to Feature #44.
- Recorded immediate engineering debt: authenticated streak-shield parity/snapshot regression, target-override validation/races, duplicate date utilities, missing unit/component harness, and snapshot growth.
- Reworked the dependency phases so completed features are no longer scheduled and high-risk economy, social, native, AI, and payment work cannot bypass their foundations.
- Preserved all prior conversation history and completed-feature implementation notes.

### 2026-07-26 — Feature #4 Implementation (Streak Shield Mechanics)

- Added `isStreakReset` helper in `src/utility/adventurePath.ts` to detect when a streak gap exceeds one day.
- Updated `claimLocalChapterReward` in `src/services/localGameRepository.ts` to increment `inventory.streakShields` by 1 on each chapter reward claim.
- Updated `completeLocalDailyQuest` to check if habit or daily streak would reset. If `streakShields > 0`, consumes exactly one shield and preserves both streaks (current + 1) instead of resetting to 1.
- Added shield pill display in `src/components/ResourceBar.tsx` (blue `shield-checkmark` icon + count, shown next to the flame only when shields > 0).
- Added shield count display in `src/screens/profile/index.tsx` next to the streak line in the profile header.
- Design decision: One shield protects all at-risk streaks (habit + daily) for a single quest completion. "Streak Protected!" toast deferred as optional future enhancement.

### 2026-07-26 — Daily Quest Reset Countdown

- Replaced the completed Daily Quest card's static next-quest subtitle with a live cooldown label in `src/components/DailyQuestCard.tsx`.
- The countdown resolves the next midnight in the user's configured timezone, incorporates the existing server clock offset, and refreshes once per second only while the card is completed or a timed quest is active.
- Kept the broader midnight rollover behavior unchanged; the app's existing provider refresh still owns the actual date transition.

### 2026-07-26 — Daily Quest Cleared Stamp

- Added generated `src/assets/images/cleared-stamp.png`, a distressed red `CLEARED` stamp with transparent bounds.
- Registered the asset in `src/constants/images.tsx` and rendered it on the completed Daily Quest card beside the completion heading.
- Kept the earned coins and XP visible below the stamp and preserved the existing completed-card copy and behavior.

### 2026-07-26 — Shared Habit Status Icon Alignment

- Added `src/components/HabitIconWithStatus.tsx` so Daily Quest, Home's habit selector, and the habit switcher share one primary-icon/status-badge arrangement.
- Moved the Daily Quest badge from the lower-left to the lower-right of the primary habit icon.
- Updated Home's habit selector to preserve each habit's primary icon while displaying completion and timed-quest status as a lower-right overlay.

### 2026-07-26 — Daily Quest Habit Status Icon Alignment

- Updated `src/components/DailyQuestCard.tsx` so the active daily quest shows the active habit icon instead of only the node icon.
- Added the same semantic completion/in-progress status badge arrangement used by the habit switcher, positioned at the lower-left of the primary icon.
- Applied the arrangement to both the active quest and completed quest card states while preserving the existing card dimensions and status colors.

### 2026-07-26 — Feature #3 Status Icon Alignment

- Updated `HabitSwitcher` status badges to preserve each habit's primary icon while overlaying Home's semantic completion/in-progress icon at the lower-right.
- Made the overlay touch-transparent so the parent habit pill remains the only accessible press target.

### 2026-07-26 — Feature #3 Implementation (Habit Quick-Switcher)

- Added `src/components/HabitSwitcher.tsx` and rendered it below the Adventure Path header in `src/screens/home/HabitPathScreen.tsx`.
- The reusable horizontal pill row changes the active habit in place, then refreshes the existing path header, chapter list, node states, and rewards through Context state.
- Pills retain the habit icon and label, with a green completion badge or gold timed-quest badge based on today's local date.
- Kept Home's 2×3 habit grid from Feature #1 as the full daily-status overview; the quick-switcher is deliberately scoped to the Adventure Path where it removes the back-to-Home navigation loop.
- Selection haptics run only when a different habit is chosen and remain controlled by the existing haptics setting.

### 2026-07-26 — Bottom Tab Haptics Correction

- Updated `BottomTabs.tsx`, `PersistentTabHost.tsx`, and `useHaptics.ts`.
- Bottom-tab feedback now uses `selectionAsync()` and fires only when the navigation host accepts an actual tab change.
- Same-tab taps and taps during an in-flight transition no longer produce false tactile confirmation.
- Haptic promise failures are safely ignored so unsupported hardware cannot interfere with navigation or quest interactions.
- Kept swipe and programmatic tab navigation silent; this correction is scoped to direct bottom-bar selection.

### 2026-07-26 — Feature Batch #3 Review

- Reviewed 6 user-proposed features against the codebase and PLANS.md. All 6 were not implemented.
- **Added as new features:**
  - **#41 Tap loot preview in celebration modal for item details (P2):** Make the existing inline loot card tappable to open `InventoryStackDetailsModal` with equip support. No new buttons — tap target is the preview itself.
  - **#42 Item catalog — all collected items (P2):** Read-only gallery in More tab showing every item definition from `discoveredItemDefinitionIds`, including lost/sold items. Grouped by set with completion progress. Distinct from Stash (equip-focused) and Stats Dashboard (#7).
- **Replaced existing feature:**
  - **#28 Custom Habit Creation → Customize Habit Settings:** Downgraded from P3 full custom habit creation to P1 habit setting overrides (type-locked, duration/count only, minimum floor). Original #28 content preserved in conversation log. Custom creation may return later as a P3 feature.
- **Updated existing feature:**
  - **#25 IKEA-Effect Onboarding:** Added explicit implementation note about guest progress migration — reading local guest cache (`"local-guest"`) and seeding Supabase state during sign-up. Explicitly coupled to #25, not a standalone feature.
- **Deferred / not added:**
  - **Community habit marketplace:** Combines #5 (Shop), #28 (Custom Habits), and #30 (Social). All three are unbuilt. Shelved until prerequisites exist.
  - **"Keep guest progress after sign-up":** Already covered by #25 — not a separate feature.

### 2026-07-26 — Feature #6 Implementation (Haptic Feedback)

- Installed `expo-haptics` (SDK 54 compatible) via `npx expo install`.
- Created `src/hooks/useHaptics.ts` — exposes `light()`, `medium()`, `heavy()` functions that read `settings.hapticsEnabled` from the new `useGameSettings()` context hook and call `Haptics.impactAsync()`.
- Added `useGameSettings()` hook to `src/contexts/appContext/index.tsx` — exposes `AppSettings` as a dedicated context slice following the existing 8-slice pattern.
- Wired haptics into 4 components:
  - `BottomTabs.tsx` — `light` on tab press
  - `DailyQuestCard.tsx` — `medium` on quest start and quest complete
  - `QuestCelebrationModal.tsx` — `medium` on loot drop collect (common–epic), `heavy` on legendary drops
  - `InventoryStackDetailsModal.tsx` — `medium` on equip/unequip
- Design decisions: ref-based `hapticsEnabled` check inside memoized callbacks avoids stale closures. Fires haptics synchronously before async actions for immediate tactile feedback.
- Future: `selection` haptic for habit switcher (#3), `heavy` for chapter complete (#36) and level-up (#12) — wired when those celebration variants exist.
- `useGameSettings()` hook also lays the foundation for Feature #8 (Settings UI).

### 2026-07-26 — Feature #2 Implementation

- Implemented energy regeneration countdown timer in `ResourceBar`.
- Added a 30-second tick interval that calculates remaining time until the next energy point (1 energy per 30 minutes).
- Displays "+1 in Nm" suffix on the energy pill when energy is below max and `lastRefillAt` is available.
- Uses `ResourcePill`'s existing `suffix` prop — no component API changes needed.
- Zero-energy fallback and low-energy warning remain as future enhancements to the feature.

### 2026-07-25 — External Recommendations Review (Batch #2)

- Reviewed a second batch of feature/QoL recommendations focused on home screen daily experience, adventure path, profile, stash, guild quests, settings, and global UX polish.
- **Already covered:** Habit completion indicators (#1 — done), daily progress banner (#1 — future enhancement), streak shield UI (#4), level-up celebration (#12), settings screen (#8), haptic feedback (#6), empty states (#22), onboarding (#14), duplicate salvage (#34).
- **Added as new features:** Chapter completion celebration (#36), equipment comparison on equip (#37), "New" badge on items (#38), pull-to-refresh + skeleton loading (#39), guild quest progress toasts + history (#40).
- **Updated existing features:** #1 (N/6 counter + loot rarity teaser), #7 (per-habit breakdown + activity log), #12 (cross-reference to chapter celebration), #15 (visual path map stretch goal).
- **Noteworthy:** The "visual path map" (board game trail aesthetic) is a high-effort/high-impact idea added as a stretch goal to #15. The animated transition between dashboard and HabitPathScreen (1c) and node detail expansion (2a) are valid polish items but too granular for standalone features — they'd be natural byproducts of #15 and #3 respectively.

### 2026-07-25 — External Recommendations Review (Batch #1)

- Reviewed a set of external feature/QoL recommendations against the existing PLANS.md.
- **Already covered:** Energy rework (#2), More tab (#7/#8), quest recovery (#3), calendar (#24), weekly recap (#23), rest days (#27), badges (#11), unused systems (Conflict Analysis), postpone list (aligns with P3 tiering).
- **Added as new features:** Post-completion flow in celebration modal (#33), duplicate gear salvage (#34), adaptive Lory messages (#35).
- **Updated existing features:** #1 (reflected actual implementation), #2 (zero-energy fallback, low-energy warning), #3 (resume quest visibility), #8 (privacy/support/export), #27 (path progress is safe messaging).
- **Added maintenance note:** Guild quest descriptions reference "four habits" but the app now has six — needs updating.

### 2026-07-25 — Feature #1 Implementation

- Implemented today-at-a-glance habit status coloring in the existing `ActiveHabitCard` habit grid (rather than a standalone strip component).
- Each habit pill in the 2×3 grid now shows: blue (active/selected), green (completed today), gold/yellow (timed quest in progress), or muted (unstarted).
- Active/selected state takes visual priority so the user always knows which habit they're viewing.
- Removed the intermediate `DailyHabitStrip` component per user feedback.

### 2026-07-25 — Feature Audit & Roadmap Session

- Conducted comprehensive codebase review across all screens, components, contexts, utilities, constants, and types.
- Identified 24 suggested features/QoL improvements with priority tiers (P0–P3).
- Reviewed user's existing 33 planned items; assessed each for quality, conflict, and alignment.
- Identified one item needing clarification: "Replace quests with gear-attribute mechanics."
- Recommended hybrid approach for post-path-completion content and loot pool management.
- Created this living `PLANS.md` document.
